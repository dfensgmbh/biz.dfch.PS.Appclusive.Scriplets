function ProcessVDIAssignment() 
{
	$result = $true;
	
	# DFTODO - Handle VDI order item
	# DFTODO - Check that a VDI doesn't exist already -> set error (Parameters??)
	# DFTODO - Assign VDI
	
	return $result;
}

function CheckForExistingVDI($username) 
{
	$result = $true;
	
	# DFTODO - Check if a VDI already exists for the given user
	
	return $result
}
